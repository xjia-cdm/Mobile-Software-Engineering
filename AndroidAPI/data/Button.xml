<api>
<api-title> Button</api-title>
<api-signature>public class Button extends TextView</api-signature>
<super-class>java.lang.Object -> android.view.View -> android.widget.TextView -> android.widget.Button</super-class>
<subclasses-direct>CompoundButton</subclasses-direct>
<subclasses-indirect>CheckBox, RadioButton, Switch, ToggleButton</subclasses-indirect>
<summary>From class <code> android.widget.TextView </code> <code> android:autoLink </code> Controls whether links such as urls and email addresses are automatically found and converted to clickable links. <code> android:autoSizeMaxTextSize </code> The maximum text size constraint to be used when auto-sizing text. <code> android:autoSizeMinTextSize </code> The minimum text size constraint to be used when auto-sizing text. <code> android:autoSizePresetSizes </code> Resource array of dimensions to be used in conjunction with <code> autoSizeTextType </code> set to <code> uniform </code> . <code> android:autoSizeStepGranularity </code> Specify the auto-size step size if <code> autoSizeTextType </code> is set to <code> uniform </code> . <code> android:autoSizeTextType </code> Specify the type of auto-size. <code> android:autoText </code> If set, specifies that this TextView has a textual input method and automatically corrects some common spelling errors. <code> android:breakStrategy </code> Break strategy (control over paragraph layout). <code> android:bufferType </code> Determines the minimum type that getText() will return. <code> android:capitalize </code> If set, specifies that this TextView has a textual input method and should automatically capitalize what the user types. <code> android:cursorVisible </code> Makes the cursor visible (the default) or invisible. <code> android:digits </code> If set, specifies that this TextView has a numeric input method and that these specific characters are the ones that it will accept. <code> android:drawableBottom </code> The drawable to be drawn below the text. <code> android:drawableEnd </code> The drawable to be drawn to the end of the text. <code> android:drawableLeft </code> The drawable to be drawn to the left of the text. <code> android:drawablePadding </code> The padding between the drawables and the text. <code> android:drawableRight </code> The drawable to be drawn to the right of the text. <code> android:drawableStart </code> The drawable to be drawn to the start of the text. <code> android:drawableTint </code> Tint to apply to the compound (left, top, etc.) drawables. <code> android:drawableTintMode </code> Blending mode used to apply the compound (left, top, etc.) drawables tint. <code> android:drawableTop </code> The drawable to be drawn above the text. <code> android:editable </code> If set, specifies that this TextView has an input method. <code> android:editorExtras </code> Reference to an <code> &lt;input-extras&gt; </code> XML resource containing additional data to supply to an input method, which is private to the implementation of the input method. <code> android:elegantTextHeight </code> Elegant text height, especially for less compacted complex script text. <code> android:ellipsize </code> If set, causes words that are longer than the view is wide to be ellipsized instead of broken in the middle. <code> android:ems </code> Makes the TextView be exactly this many ems wide. <code> android:fallbackLineSpacing </code> Whether to respect the ascent and descent of the fallback fonts that are used in displaying the text. <code> android:firstBaselineToTopHeight </code> Distance from the top of the TextView to the first text baseline. <code> android:fontFamily </code> Font family (named by string or as a font resource reference) for the text. <code> android:fontFeatureSettings </code> Font feature settings. <code> android:freezesText </code> If set, the text view will include its current complete text inside of its frozen icicle in addition to meta-data such as the current cursor position. <code> android:gravity </code> Specifies how to align the text by the view's x- and/or y-axis when the text is smaller than the view. <code> android:height </code> Makes the TextView be exactly this tall. <code> android:hint </code> Hint text to display when the text is empty. <code> android:hyphenationFrequency </code> Frequency of automatic hyphenation. <code> android:imeActionId </code> Supply a value for <code> EditorInfo.actionId </code> used when an input method is connected to the text view. <code> android:imeActionLabel </code> Supply a value for <code> EditorInfo.actionLabel </code> used when an input method is connected to the text view. <code> android:imeOptions </code> Additional features you can enable in an IME associated with an editor to improve the integration with your application. <code> android:includeFontPadding </code> Leave enough room for ascenders and descenders instead of using the font ascent and descent strictly. <code> android:inputMethod </code> If set, specifies that this TextView should use the specified input method (specified by fully-qualified class name). <code> android:inputType </code> The type of data being placed in a text field, used to help an input method decide how to let the user enter text. <code> android:justificationMode </code> Mode for justification. <code> android:lastBaselineToBottomHeight </code> Distance from the bottom of the TextView to the last text baseline. <code> android:letterSpacing </code> Text letter-spacing. <code> android:lineHeight </code> Explicit height between lines of text. <code> android:lineSpacingExtra </code> Extra spacing between lines of text. <code> android:lineSpacingMultiplier </code> Extra spacing between lines of text, as a multiplier. <code> android:lines </code> Makes the TextView be exactly this many lines tall. <code> android:linksClickable </code> If set to false, keeps the movement method from being set to the link movement method even if autoLink causes links to be found. <code> android:marqueeRepeatLimit </code> The number of times to repeat the marquee animation. <code> android:maxEms </code> Makes the TextView be at most this many ems wide. <code> android:maxHeight </code> Makes the TextView be at most this many pixels tall. <code> android:maxLength </code> Set an input filter to constrain the text length to the specified number. <code> android:maxLines </code> Makes the TextView be at most this many lines tall. <code> android:maxWidth </code> Makes the TextView be at most this many pixels wide. <code> android:minEms </code> Makes the TextView be at least this many ems wide. <code> android:minHeight </code> Makes the TextView be at least this many pixels tall. <code> android:minLines </code> Makes the TextView be at least this many lines tall. <code> android:minWidth </code> Makes the TextView be at least this many pixels wide. <code> android:numeric </code> If set, specifies that this TextView has a numeric input method. <code> android:password </code> Whether the characters of the field are displayed as password dots instead of themselves. <code> android:phoneNumber </code> If set, specifies that this TextView has a phone number input method. <code> android:privateImeOptions </code> An addition content type description to supply to the input method attached to the text view, which is private to the implementation of the input method. <code> android:scrollHorizontally </code> Whether the text is allowed to be wider than the view (and therefore can be scrolled horizontally). <code> android:selectAllOnFocus </code> If the text is selectable, select it all when the view takes focus. <code> android:shadowColor </code> Place a blurred shadow of text underneath the text, drawn with the specified color. <code> android:shadowDx </code> Horizontal offset of the text shadow. <code> android:shadowDy </code> Vertical offset of the text shadow. <code> android:shadowRadius </code> Blur radius of the text shadow. <code> android:singleLine </code> Constrains the text to a single horizontally scrolling line instead of letting it wrap onto multiple lines, and advances focus instead of inserting a newline when you press the enter key. <code> android:text </code> Text to display. <code> android:textAllCaps </code> Present the text in ALL CAPS. <code> android:textAppearance </code> Base text color, typeface, size, and style. <code> android:textColor </code> Text color. <code> android:textColorHighlight </code> Color of the text selection highlight. <code> android:textColorHint </code> Color of the hint text. <code> android:textColorLink </code> Text color for links. <code> android:textIsSelectable </code> Indicates that the content of a non-editable text can be selected. <code> android:textScaleX </code> Sets the horizontal scaling factor for the text. <code> android:textSize </code> Size of the text. <code> android:textStyle </code> Style (normal, bold, italic, bold|italic) for the text. <code> android:typeface </code> Typeface (normal, sans, serif, monospace) for the text. <code> android:width </code> Makes the TextView be exactly this wide.</summary>
</api>